package en;

public class Enum {
    public enum Direction{
        HORIZ,
        VERT
    }

    public enum Place{
        START,
        END,
        INNER
    }

    public enum Status{
        DISABLE,
        ENABLE
    }
}
